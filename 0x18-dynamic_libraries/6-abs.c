#include "main.h"
/**
 * _abs - The function computes the absolute value of an int
 * @n: The character being checked
 *
 * Return: abs of num
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-1 * n);
	}
}
